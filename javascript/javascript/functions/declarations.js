// function declaration
function soma(x, y) {
  // js interprets before execution
  return x + y
}

// function expression
const sub = function (x, y) {
  return x - y
}

// named function expression
const mult = function mult(x, y) {
  return x * y
}
